#include <string.h>

void	*ft_memset(void *s, int c, size_t n)
{
/*	size_t	i;
	unsigned char	c1;
	char	*str;

	str = (char*)s;
	c1 = (unsigned char)c;
	i = 0;
	while (i < n && str[i])
	{
		str[i] = c1;
		i++;
	}
*/	return (s);
}
